import Link from "next/link"; import { AppShell } from "@/components/AppShell"; import { requireMenuAccess } from "@/lib/permissions"; import { data } from "@/lib/data";
export default async function Student(){const a=await requireMenuAccess("portal.dashboard");const stats=await data.portalDashboard();return <AppShell actor={a}><div className="content-head"><div><div className="eyebrow">Cổng học sinh</div><h1>Chào {a.name}</h1></div><Link className="button" href="/hoc-tap/bai-tap">Làm bài tập</Link></div><div className="stats"><div className="card stat"><strong>{stats.classes}</strong><span>Lớp đang học</span></div><div className="card stat"><strong>{stats.todo}</strong><span>Bài cần làm</span></div><div className="card stat"><strong>{stats.done}</strong><span>Bài đã nộp</span></div><div className="card stat"><strong>{stats.resources}</strong><span>Tài liệu</span></div></div><section className="section"><div className="card"><h2>Tiến độ của em</h2><p>Mở danh sách bài tập để bắt đầu hoặc tiếp tục bài đang làm. Câu trả lời được lưu xuống server trong quá trình làm.</p></div></section></AppShell>}
