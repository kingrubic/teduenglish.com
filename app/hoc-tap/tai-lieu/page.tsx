import { AppShell } from "@/components/AppShell";import { requireMenuAccess } from "@/lib/permissions";import { data } from "@/lib/data";
export default async function MyResources(){const a=await requireMenuAccess("portal.resources");const x=await data.myResources();return <AppShell actor={a}><div className="content-head"><div><div className="eyebrow">Đúng quyền truy cập</div><h1>Tài liệu của em</h1></div></div><div className="data-list">{x.rows.map(r=><div className="data-row" key={r.id}><div><strong>{r.title}</strong><div className="muted">{r.description}</div></div><a className="button secondary" href={`/api/tai-lieu/${r.id}`}>Tải tài liệu</a></div>)}</div></AppShell>}
