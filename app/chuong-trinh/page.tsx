import Link from "next/link"; import { PublicHeader } from "@/components/PublicHeader"; import { data } from "@/lib/data";
export default async function Programs(){const p=await data.publishedPrograms();return <><PublicHeader/><section className="section"><div className="container"><div className="eyebrow">Chương trình học</div><h1>Chọn lộ trình phù hợp</h1><div className="grid-3">{p.rows.map(x=><Link className="card" key={x.id} href={`/chuong-trinh/${x.slug}`}><span className="tag">{x.audience}</span><h3>{x.title}</h3><p>{x.summary}</p><strong>Xem chi tiết →</strong></Link>)}</div></div></section></>}
