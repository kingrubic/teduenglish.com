import { notFound } from "next/navigation"; import Link from "next/link"; import { PublicHeader } from "@/components/PublicHeader"; import { data } from "@/lib/data";
export default async function Program({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const p=await data.programBySlug(slug);const x=p.rows[0];if(!x)notFound();return <><PublicHeader/><section className="hero"><div className="container"><span className="tag">{x.audience}</span><h1>{x.title}</h1><p>{x.summary}</p><Link className="button" href="/dang-ky-tu-van">Đăng ký tư vấn</Link></div></section><section className="section white"><div className="container"><h2>Đầu ra chương trình</h2><p>{x.outcomes}</p></div></section></>}
