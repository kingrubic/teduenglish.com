import { AppShell } from "@/components/AppShell"; import { requireMenuAccess } from "@/lib/permissions"; import { data } from "@/lib/data";
export default async function Cms(){const a=await requireMenuAccess("cms.dashboard");const stats=await data.cmsDashboard();return <AppShell actor={a}><div className="content-head"><div><div className="eyebrow">Teacher CMS</div><h1>Tổng quan vận hành</h1></div></div><div className="stats"><div className="card stat"><strong>{stats.classes}</strong><span>Lớp học</span></div><div className="card stat"><strong>{stats.students}</strong><span>Học sinh</span></div><div className="card stat"><strong>{stats.pending}</strong><span>Bài cần chấm</span></div><div className="card stat"><strong>{stats.inquiries}</strong><span>Đăng ký mới</span></div></div><section className="section"><h2>Vertical slice đang hoạt động</h2><div className="card"><p>Tạo lớp, quản lý enrollment, giao bài, học sinh autosave/nộp bài, chấm tự động và xem kết quả đều dùng Convex local backend và kiểm tra quyền ở server.</p></div></section></AppShell>}
