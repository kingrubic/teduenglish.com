import { AppShell } from "@/components/AppShell"; import { requireMenuAccess } from "@/lib/permissions"; import { data } from "@/lib/data";
export default async function Inquiries(){const a=await requireMenuAccess("cms.inquiries");const x=await data.inquiries();return <AppShell actor={a}><div className="content-head"><h1>Đăng ký tư vấn</h1></div>{x.rows.length?<div className="data-list">{x.rows.map(v=><div className="data-row" key={v.id}><div><strong>{v.full_name}</strong><div className="muted">{v.phone} · {v.current_level}</div></div><span className="tag">{v.status}</span></div>)}</div>:<div className="card">Chưa có đăng ký mới.</div>}</AppShell>}
