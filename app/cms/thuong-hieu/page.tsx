import { AppShell } from "@/components/AppShell"; import { requireMenuAccess } from "@/lib/permissions"; import { updateSiteSettings } from "@/lib/actions"; import { getSiteSettings } from "@/lib/site";
export default async function BrandSettings(){const a=await requireMenuAccess("cms.brand");const s=await getSiteSettings();return <AppShell actor={a}><div className="content-head"><div><div className="eyebrow">Earth Academic</div><h1>Thương hiệu & hồ sơ giáo viên</h1></div></div><div className="card"><form className="form" action={updateSiteSettings}><label>Tên thương hiệu<input name="brandName" defaultValue={s.brandName}/></label><label>Tagline<input name="tagline" defaultValue={s.tagline}/></label><label>Tên giáo viên<input name="teacherName" defaultValue={s.teacherName}/></label><label>Chức danh<input name="teacherTitle" defaultValue={s.teacherTitle}/></label><label>Đơn vị công tác<input name="teacherOrganization" defaultValue={s.teacherOrganization}/></label><label>Giới thiệu ngắn<textarea name="teacherShortBio" defaultValue={s.teacherShortBio}/></label><label>Giới thiệu đầy đủ<textarea name="teacherLongBio" defaultValue={s.teacherLongBio} style={{minHeight:260}}/></label><div className="class-meta"><label>Email liên hệ<input name="contactEmail" type="email" defaultValue={s.contactEmail||""}/></label><label>Số điện thoại<input name="contactPhone" defaultValue={s.contactPhone||""}/></label></div><label>SEO title<input name="seoTitle" defaultValue={s.seoTitle}/></label><label>SEO description<textarea name="seoDescription" defaultValue={s.seoDescription}/></label><div className="card"><strong>Ảnh chân dung</strong><p className="muted">Khuyến nghị 4:5, tối thiểu 1200×1500 px, JPG/WebP, tối đa 5 MB. Chức năng upload object storage sẽ được kích hoạt khi storage provider được cấu hình; hiện website dùng placeholder trung tính.</p></div><div className="card"><strong>Logo & favicon</strong><p className="muted">Logo SVG/PNG nền trong suốt; favicon vuông tối thiểu 512×512 px, tối đa 2 MB.</p></div><button className="button accent">Lưu và cập nhật website</button></form></div></AppShell>}
