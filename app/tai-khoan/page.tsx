import { AppShell } from "@/components/AppShell";import { changePassword } from "@/lib/actions";import { requireActor } from "@/lib/auth";
export default async function Account(){const actor=await requireActor();return <AppShell actor={actor}><div className="content-head"><div><div className="eyebrow">Bảo mật tài khoản</div><h1>Đổi mật khẩu</h1></div></div><section className="card account-security"><form className="form" action={changePassword}><label>Mật khẩu hiện tại<input name="currentPassword" type="password" minLength={8} required/></label><label>Mật khẩu mới<input name="newPassword" type="password" minLength={8} required/></label><button className="button">Đổi mật khẩu và đăng xuất thiết bị</button></form></section></AppShell>}
